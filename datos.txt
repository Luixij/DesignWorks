COMANDO ENTRAR MARIA DB CON DATOS ACTUALIZADO.
Credenciales establecidad en .env de /infra

docker exec -it designWorks_mariadb mariadb -u dsing_user -pFcfR_El21 design_works


COMANDOS BASE DE DATOS mariadb

SHOW TABLES;
SELECT COUNT(*) FROM usuarios;
SELECT COUNT(*) FROM trabajos;
SELECT * FROM trabajos ORDER BY id;

GESTOR ADMINER BASE DE DATOS
http://localhost:8081

Servidor: mariadb
Usuario: dsing_user
Pass: FcfR_El21
Base datos: design_works

---------------------------------------

DOCKER

User: luisscj678@gmail.com
Pass: Car1322.4502Tw

------------------------------------------

CONECTAR SPRING BOOT CON BASE DE DATOS
Variables definidas dentro del .env dentro de /backend

En Mac / Linux:
Desde la carpeta backend/:

export DB_URL=jdbc:mariadb://127.0.0.1:3306/design_works
export DB_USER=dsing_user
export DB_PASS=FcfR_El21

./mvnw spring-boot:run


En Windows (PowerShell)
$env:DB_URL="jdbc:mariadb://127.0.0.1:3306/design_works"
$env:DB_USER="dsing_user"
$env:DB_PASS="FcfR_El21"

.\mvnw.cmd spring-boot:run

PARA INICIAR SPRING BOOT
ESTO FUNCIONA DESDE EL TERMINAL DE VISUAL STUDIO CODE EN LA CARPETA backend/backend
 mvn "-Dspring-boot.run.profiles=local" spring-boot:run

EN WINDOWS IMPORTANTE TENER EN CUENTA QUE USO EL PUERTO 8082 Y EN MAC EL 8080
 ABRIR SPRING BOOT EN localhost: http://localhost:8082/
    name: admin
    password: d.MCmq2des

Se define un launch.json dentro de .vscode para ejecutar directamente
las variables necesarias para conectar springboot con mariadb.
No he subido a git, he subido el archivo de ejemplo "launch.example.json"
en /backend

-----------------------------------------------------

GIT - SINCRONIZAR REPOSITORIO CENTRAL CON LOCALES EN Mac Y Windows

Subir cambios al repo CENTRAL
git push origin main

Traer cambios del repo CENTRAL
git pull --ff-only origin main

Ver últimos cambios
git log --oneline --max-count=5


-----------------------------------------------------

FLUTTER

Importante tener en cuenta que en mac se usa el puerto 8080 y en windows 8082

flutter run --no-enable-impeller --dart-define=API_BASE_URL=http://10.0.2.2:8080